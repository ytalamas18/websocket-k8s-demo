apiVersion: v1
kind: Service
metadata:
  name: websocket-service
spec:
  type: NodePort
  selector:
    app: websocket-app
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
    nodePort: 30080